import { createApp } from "vue";
import "@mf-demo/ui/styles.css";
import BillingPage from "./BillingPage.vue";

// Standalone mount (for independent development)
createApp(BillingPage).mount("#app");
